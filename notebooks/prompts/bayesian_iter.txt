We are fitting a concept bottleneck model (CBM) to predict influenza (flu). We have {num_concepts_fixed} meta-concepts so far:
{meta_concepts}

To improve the CBM, these are the top phrases from the residual model:
{top_features_df}

A positive coefficient means higher flu risk. A negative coefficient means lower flu risk.

Create candidates for the {num_concepts}th meta-concept. Be systematic and consider all listed concepts.

Propose at least ten candidates in JSON format:
{
  "reasoning": "Step-by-step explanation",
  "concepts": [
    {
      "concept": "Does the note mention the patient having body aches?",
      "is_risk_factor": true,
      "words": ["body aches", "myalgias", "muscle pain"]
    }
  ]
}
